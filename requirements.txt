# ── Core Streamlit & environment ─────────────────────────────────────────────
streamlit
python-dotenv

# ── rag-core (shared RAG pipeline) ───────────────────────────────────────────
# Install as editable so careerbot can import rag_core directly.
# From the careerbot project root run:
#   pip install -e ../rag-core
# Or if rag-core is published to PyPI:
#   rag-core

# ── LLM / LangChain stack ────────────────────────────────────────────────────
# These are also declared in rag-core/requirements.txt but listed here
# so careerbot can be installed standalone without rag-core's pyproject.
groq
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq
langchain-huggingface

# ── Vector store & embeddings ─────────────────────────────────────────────────
chromadb==1.5.9
langchain-chroma==1.1.0
sentence-transformers

# ── PDF loading (text-based PDFs) ─────────────────────────────────────────────
pypdf

# ── OCR stack ─────────────────────────────────────────────────────────────────
# Requires system packages:
#   sudo apt-get install -y tesseract-ocr poppler-utils
pytesseract
pdf2image
Pillow

# ── Semantic chunking ─────────────────────────────────────────────────────────
langchain-experimental

# ── Evaluation & data ─────────────────────────────────────────────────────────
ragas
datasets
pandas

# ── Tokenizer ─────────────────────────────────────────────────────────────────
tokenizers==0.21.1

# ── Telemetry / protobuf compatibility ────────────────────────────────────────
protobuf>=4.25.3,<5.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
opentelemetry-sdk>=1.27.0
git+https://github.com/konduri-lakshmi-prasanna/RAG_pipeline.git

# ── REMOVED from original requirements.txt ───────────────────────────────────
# rank-bm25         ← BM25 hybrid search now handled inside rag-core
# (HybridRetriever in careerbot is deleted; rag-core's rerank stage
#  uses RRF without needing rank-bm25 as a careerbot dependency)